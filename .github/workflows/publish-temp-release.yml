name: Publish stable release

on:
  push:
    paths:
      - ".github/workflows/publish-temp-release.yml"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Publish v1.10.0
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p release-assets
          cd release-assets
          gh release download latest --repo "albaintor/integration-zidoo" --pattern "uc-intg-zidoo-v1.10.0-aarch64-*.tar.gz"
          src="$(find . -maxdepth 1 -type f -name 'uc-intg-zidoo-v1.10.0-aarch64-*.tar.gz' | sort | tail -n 1)"
          test -n "$src"
          stable="uc-intg-zidoo-v1.10.0-aarch64.tar.gz"
          mv "$src" "$stable"
          sha256sum "$stable" | sed 's#  #  #' > "uc-src.hash"
          gh release create "v1.10.0" --repo "albaintor/integration-zidoo" --title "v1.10.0" \
            --notes "Fix setup flow so starting or aborting setup no longer clears an existing shared configuration. Existing devices are preserved unless the explicit reset action is selected." \
            "$stable" "uc-src.hash"
